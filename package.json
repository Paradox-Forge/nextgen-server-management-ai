{
  "name": "nextgen-server-management",
  "version": "1.0.0",
  "description": "NextGen Guild Management - Monorepo Control",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm run build --prefix extension\" \"npm run dev --prefix backend\"",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix extension\"",
    "install:all": "npm run install:backend && npm run install:extension",
    "install:backend": "npm install --prefix backend",
    "install:extension": "npm install --prefix extension",
    "build:extension": "npm run build --prefix extension",
    "clean": "rimraf backend/node_modules extension/node_modules dist"
  },
  "keywords": [],
  "author": ["Burak Behlül", "Paradox Forge"],
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "rimraf": "^5.0.5"
  }
}
